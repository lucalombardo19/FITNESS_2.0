# Genera con: openssl rand -base64 32
NEXTAUTH_SECRET=cambia-questa-stringa-con-una-sicura
NEXTAUTH_URL=http://localhost:3000

# Opzionale: se imposti qui le keys, si usano come fallback per tutti gli utenti
# ANTHROPIC_API_KEY=sk-ant-api03-...
# USDA_API_KEY=...
